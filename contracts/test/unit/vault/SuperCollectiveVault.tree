SuperCollectiveVault.t.sol
├── given no tokens are locked
│   ├── when lock is called without prior approval
│   │   └── it should revert with INSUFFICIENT_ALLOWANCE
│   └── when lock is called with sufficient allowance
│       └── it should lock the tokens
├── given tokens are locked
│   ├── when unlock is called by an unauthorized user
│   │   └── it should revert with UNAUTHORIZED
│   ├── when unlock is called with a valid amount
│   │   └── it should unlock the tokens
│   └── when unlock is called with an amount exceeding locked balance
│       └── it should revert with INSUFFICIENT_BALANCE
├── given rewards distribution is enabled
│   ├── when a valid Merkle proof is provided
│   │   └── it should distribute rewards to the user
│   ├── when an invalid Merkle proof is provided
│   │   └── it should revert with INVALID_MERKLE_PROOF
│   ├── when rewards are already distributed
│   │   └── it should revert with ALREADY_CLAIMED
│   └── when a mismatched reward token is used
│       └── it should revert with INVALID_TOKEN
└── given Merkle root updates are required
    ├── when updated by an authorized user
    │   └── it should update the Merkle root
    └── when updated by an unauthorized user
        └── it should revert with UNAUTHORIZED
